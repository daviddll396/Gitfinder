import React, { Fragment } from 'react'
import spinner from './spinner.gif';

const Spinner = () => <Fragment>
      <div className="loading .all-center" style={{ width: '200px', margin: 'auto', display: 'block' }} >
        Loading...
      </div>
    </Fragment>

export default Spinner
